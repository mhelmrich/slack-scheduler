const mongoose = require("mongoose");
var Schema = mongoose.Schema;



/*
var MeetingSchema = new Schema (
{
  day: {
    type: String,
    required: true
  },
  time: {
    type: Date,
    required: true
  },
  invitees: [{
    type: String,//double check. Will need to be users
    required: true
  }],
  subject: String,
  location: String,//string, set of coordinates, what???
  meetingLength: Number,
  googleCalendarFields: String,//What even is this?
  status: String,
  createdAt: Date,
  requesterId: String
});


var Meeting = mongoose.model("Meeting", MeetingSchema);


module.exports = {Meeting: Meeting};*/
